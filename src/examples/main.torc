action print_character(name, age) {
    state character = Object(name: name, edad: 15 + age);

    print character;
}

print_character("Juan", 15);

state characters = List(Object(name: "Mateo", edad: 16 + 3));

print characters;